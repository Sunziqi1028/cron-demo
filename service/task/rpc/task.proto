syntax = "proto3";

package task;

option go_package = "./task";

message Req{
  int64 id = 1;
}

message TaskReply{
  int64 id = 1;
  string message = 2;
}

service Task {
  rpc TaskToMonitor(Req) returns(TaskReply);
}