type (
	Request {
		StartTime string `json:"startTime"`
		Duration  string `json:"duration"`
		Task      string `json:"task"`
	}

	Response {
		Code    int    `json:"code"`
		Message string `json:"message"`
	}
)

service task {
	@handler taskHandler
	post /task (Request) returns (Response)
}